{
  "name": "jwt-signup",
  "version": "1.0.0",
  "engines": {
    "node": "10.5.0",
    "npm": "6.9.0",
    "yarn": "1.13.0"
  },
  "scripts": {
    "start": "cd server && yarn && cd .. && node server/bin/www",
    "heroku-postbuild": "cd client && yarn && yarn build && cd .."
  }
}